export interface User {
    id: string
    name: string
    email: string
    role: string
    avatar: string
  }

  export interface Post {
    id: number
    title: string
    excerpt: string
    author: string
    date: string
  }
